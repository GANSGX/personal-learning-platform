# Curriculum

Source of truth for lessons. Files are MDX with Zod-validated frontmatter. Do not store lessons in a database.

`content/fixtures/` holds two placeholder nodes so the canvas has something to lay out before Networking I.
